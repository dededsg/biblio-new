extends layout

block content

  h1 Deletar gênero: #{genre.name}

  if genre_books.length

    p
      strong Exclua os seguintes livros antes de tentar excluir este gênero.

    div(style='margin-left:20px;margin-top:20px')

      h4 Livros

      dl
      each book in genre_books
        dt
          a(href=book.url) #{book.title}
        dd #{book.summary}

  else
    p Você tem certeza da sua ação?

    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=genre._id )

      button.btn.btn-primary(type='submit') Deletar
